# Application configuration
app:
  name: yolo-detection
  image:
    repository: luminoussg/yolo-detection
    tag: v1
    pullPolicy: IfNotPresent
  
  # Resource limits and requests
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 500m
      memory: 512Mi

# Replica count and autoscaling configuration
replicaCount: 1
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

# Service configuration
service:
  type: LoadBalancer
  port: 80
  loadBalancerIP: "192.168.31.197"

# Pod configurations
podAnnotations: {}
nodeSelector: {}
tolerations: []
affinity: {} 